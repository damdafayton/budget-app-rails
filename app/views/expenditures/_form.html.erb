<%= form_with(model: expenditure)  do |form| %>
  <% if expenditure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expenditure.errors.count, "error") %> prohibited this expenditure from being saved:</h2>

      <ul>
        <% expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group, style: "display: block", class:'form-label' %>
    <%= form.select :group.name, Group.where(author_id: current_user.id).pluck(:name, :id),{},{class:'form-control'}%>
  </div>

  <div>
    <%= form.label :name, style: "display: block", class:'form-label' %>
    <%= form.text_field :name, class:'form-control' %>
  </div>

  <div class=''>
    <%= form.label :amount, style: "display: block", class:'form-label' %>
    <%= form.text_field :amount, class:'form-control' %>
  </div>

  <div>
    <%= form.submit 'Save', class: 'btn btn-success text-white my-2' %>
  </div>
<% end %>
